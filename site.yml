---
- hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: 打印测试信息
      debug:
        msg: "Hello Semaphore! Playbook can run successfully."

    - name: 测试创建一个临时文件
      file:
        path: /tmp/semaphore_test.txt
        state: touch
